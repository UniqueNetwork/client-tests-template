# Example app

0. Install node modules
    ```shell
    npm i
    ```

2. Build application using 
    ```shell
    npm i && npm run build
    ```

2. Run server on localhost:8000 using
    ```shell
    npm run serve
    ```